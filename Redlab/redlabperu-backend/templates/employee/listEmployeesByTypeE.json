{
  "status": true,
  "data": {
    "{{#each employees}}": {
      "id": "{{#? this.id}}",
      "birthDate": "{{#? this.birthDate}}",
      "admissionDate": "{{#? this.admissionDate}}",
      "birthDateUS": "{{#? this.birthDateUS}}",
      "admissionDateUS": "{{#? this.admissionDateUS}}",
      "name": "{{#? this.name}}",
      "lastNameP": "{{#? this.lastNameP}}",
      "lastNameM": "{{#? this.lastNameM}}",
      "phoneNumber": "{{#? this.phoneNumber}}",
      "tlfNumber": "{{#? this.tlfNumber}}",
      "gender": "{{#? this.gender}}",
      "genderStr": "{{#? this.genderStr}}",
      "civilStatus": "{{#? this.civilStatus}}",
      "civilStatusStr": "{{#? this.civilStatusStr}}",
      "digitalSignatureUrl": "{{#? this.digitalSignatureUrl}}",
      "address": "{{#? this.address}}",
      "typeEmployee": {
        "id": "{{#? this.TypeEmployee.id}}",
        "name": "{{#? this.TypeEmployee.name}}"
      }
    }
  }
}