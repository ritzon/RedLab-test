{
  "status": true,
  "data": {
    "id": "{{#? headquarter.id}}",
    "name": "{{#? headquarter.name}}",
    "address": "{{#? headquarter.address}}",
    "tlfNumber": "{{#? headquarter.tlfNumber}}",
    "urlImage": "{{#? headquarter.urlImage}}"
  }
}